# Minimal docker repository containing jupyter, julia and python3 (+numpy, scipy, pandas and matplotlib)

To build the docker container one can use ```run.sh``` (use ```chmod +x run.sh``` and then ```./run.sh```).
